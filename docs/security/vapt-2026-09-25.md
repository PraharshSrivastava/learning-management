# VAPT triage: 2026-09-25

Source: `VAPT_REPORT.md` generated at `20260925-112955` for learning-management.
The report includes SAST, dependency, secret, Dockerfile, and container-image scans.
Its source checkout is not identified by commit; findings were checked against this repository before changes were made.

## Changes made

| Area | Action |
| --- | --- |
| SMTP transport | SMTP mode now requires STARTTLS or implicit TLS. Both use a certificate-verifying context with hostname checks. TLS failure prevents authentication and delivery. |
| Nginx upgrade headers | Only an exact WebSocket upgrade is passed upstream. Other client-supplied upgrade values, including `h2c`, are dropped. |
| Container images | Python, Nginx, Postgres, and Ubuntu build-stage images were updated to verified tags and pinned digests. Backend OS package version pins were removed so the build installs current security updates from the base distribution. |
| Backend container user | The runtime user now has a numeric UID and GID. |

## Static-analysis triage

| Report item | Decision | Evidence |
| --- | --- | --- |
| Semgrep credential leak, `providers.py:185` | False positive | The logged values are a course ID, stage, and token count. No credential is formatted into that message. |
| Semgrep and Bandit XML import, `pdf_builder.py:8` | False positive | `xml.sax.saxutils.escape` escapes text for ReportLab paragraphs; it does not parse XML or resolve external entities. |
| Semgrep dynamic urllib, `e2e_employee_smoke.py:55` | False positive | The test script checks for HTTP and restricts hostnames to `127.0.0.1` or `localhost` before calling `urlopen`. |
| Semgrep Nginx h2c, both frontend configurations | Fixed | Upgrade forwarding now accepts only the WebSocket token. |
| Bandit B608, three locations in `employees.py` | False positive | SQL fragments contain only generated `?` placeholders; employee IDs are passed separately as bound parameters. |
| Bandit B608, `saved_assignment_groups.py:56` | False positive | The optional clause is a fixed string, `AND group_type = ?`; the supplied value is a bound parameter. |
| Bandit B105, `hub_launch.py:121` (fresh scan) | False positive | `lms_session` is a token type label in the signed payload, not a password or secret. |
| Secret patterns, seven matches | False positives | Matches are variable names and assignments involving tokens or keys, not credential values. |
| Sensitive file, `.env.example` | Expected | The tracked file is a documented template with placeholders and an empty `SMTP_PASSWORD`. Live `.env` files are ignored by Git. |
| Hadolint DL3025, three Dockerfiles | Stale report | Current `CMD` declarations already use JSON-array form. |
| Hadolint DL3066, three Dockerfiles | Addressed | Both frontends already use numeric `USER 101`; backend now uses `USER 10001:10001`. |

## Verification and release gate

Trivy 0.74.0 was run on built runtime images, using one vulnerability database for the old and new images. These numbers are not directly comparable to the source report's 690 findings, which scanned declared base images and a build-stage Ubuntu image rather than the deployed runtime images.
For example, the report counted 135 findings in the old Nginx base image, while the old built frontend image had zero after its `apk upgrade` build step.

| Runtime image | Old build | Updated build | Remaining critical alerts |
| --- | ---: | ---: | --- |
| Backend | 975 | 795 | 1: `CVE-2026-6653` in Debian `libxml2` |
| Trainer frontend | 0 | 0 | 0 |
| Employee frontend | 0 | 0 | 0 |
| Postgres 16 | 431 | 330 | 2: `CVE-2026-6653` in `libxml2`; `CVE-2025-68121` in the `gosu` Go binary |

Debian's [CVE-2026-6653 tracker](https://security-tracker.debian.org/tracker/CVE-2026-6653) marks Trixie vulnerable with no security advisory and calls the issue minor. Trivy has no fixed Debian version for this image. The `gosu` alert concerns Go TLS certificate validation; `gosu` is used to switch process identity and does not initiate TLS connections. Keep both alerts visible for subsequent upstream updates instead of suppressing them from the scan.

The exact `requirements.lock` was audited with `pip-audit`; no known Python dependency vulnerabilities were found. Both frontend builds and the backend build completed, `nginx -t` passed for both frontend images, and Hadolint 2.14.0 reported no Dockerfile findings. Trivy's configuration scan reported zero findings in the three Dockerfiles, and its secret scan reported zero findings in the changed files. A fresh Semgrep security-audit scan reported two findings (XML import and local test URL), both triaged above. A fresh Bandit scan reported six findings, all triaged above. The email tests and targeted Ruff checks passed. The full backend suite has one existing, unrelated failure: `test_splits_third_landscape_image_to_separate_slide` expects three slides and receives four. None of the files involved in that test changed here.

Before release:

- Resolve or separately track the image-slide test failure before using a fully green backend suite as a release gate.
- Record scan outputs with the final build digests as part of the release evidence.
- Verify SMTP against the intended server in staging, including a rejected invalid certificate. Confirm normal API and employee WebSocket traffic through Nginx.
